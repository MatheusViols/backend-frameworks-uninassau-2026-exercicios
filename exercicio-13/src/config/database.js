// TODO: Configurar conexão com o banco de dados usando Sequelize
// Dica: Use SQLite para facilitar o desenvolvimento
// Exporte a instância do Sequelize para ser usada nos models

const { Sequelize } = require('sequelize');

// Crie a instância do Sequelize aqui
// const sequelize = new Sequelize(...)

module.exports = sequelize;
