// TODO: Criar o model User com Sequelize
// Campos: id, name, email, age, createdAt, updatedAt
// Validações: name e email obrigatórios, email único

const { DataTypes } = require('sequelize');
const sequelize = require('../config/database');

// Defina o model User aqui
// const User = sequelize.define('User', { ... });

module.exports = User;
