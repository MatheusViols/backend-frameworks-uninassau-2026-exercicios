// TODO: Implemente o ProductController aqui
//
// O controller deve ter os seguintes métodos:
//
// - index(req, res): renderizar view 'index' com lista de produtos
// - create(req, res): renderizar view 'create' (formulário)
// - store(req, res): processar POST, criar produto, redirecionar para '/'
// - destroy(req, res): deletar produto, redirecionar para '/'
//
// Dicas:
// - Use Product.findAll() para buscar produtos
// - Use Product.create(req.body) para criar
// - Use res.render('view', { data }) para renderizar
// - Use res.redirect('/') para redirecionar
// - Trate erros de validação

