// TODO: Implemente Model User com persistência em JSON
//
// const fs = require('fs');
// const path = require('path');
//
// const filePath = path.join(__dirname, '../data/users.json');
//
// Métodos necessários:
// - loadUsers(): ler arquivo JSON, retornar array (ou [] se não existir)
// - saveUsers(users): salvar array no arquivo
// - findAll(): retornar todos os usuários
// - findById(id): buscar por ID
// - create({ name, email }): criar usuário
// - update(id, data): atualizar usuário
// - delete(id): deletar usuário

