// TODO: PostController com Mongoose
// const Post = require('../models/Post');
//
// index: Post.find().populate('author')
// show: Post.findById(id).populate('author')
// store: Post.create(req.body)
// update: Post.findByIdAndUpdate(id, data, { new: true })
// destroy: Post.findByIdAndDelete(id)

